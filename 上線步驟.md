# 履歷網站上線步驟｜Cloudflare Pages ＋ Pages CMS

做完你會得到：
- 一個**永久網址**（可以貼在履歷、104、IG bio）
- 一個**線上後台**：登入 → 改字換圖 → 按儲存 → 網站自動更新，不用下載任何東西
- 全部**免費**，沒有廣告

⚠ 註冊帳號的步驟必須你自己做（我不能代為註冊或輸入密碼）。設定檔我都準備好了。

---

## 你會用到的三個免費服務

| 服務 | 做什麼 | 要註冊嗎 |
|---|---|---|
| **GitHub** | 存放檔案（相當於雲端硬碟） | 要 |
| **Cloudflare Pages** | 把檔案變成網站、給你網址 | 要 |
| **Pages CMS** | 你的後台介面 | 不用，用 GitHub 帳號登入 |

---

## 步驟一：註冊 GitHub 並建立 repository

1. 到 **github.com** → Sign up，用 `tonytsaidx@gmail.com` 註冊
2. 登入後點右上角 **＋** → **New repository**
3. 設定：
   - **Repository name**：`resume`
   - 選 **Public**（免費方案用 Public 才能上 Pages CMS）
   - **不要**勾選任何 Add a README／.gitignore
4. 按 **Create repository**

## 步驟二：把檔案上傳上去

1. 在剛建好的 repo 頁面點 **uploading an existing file**
2. **把 `履歷網站-上線版` 資料夾裡的東西全部拖進去**，包含：
   - `index.html`
   - `content.json`
   - `.pages.yml`　← ⚠ **這個檔案開頭是點，很容易漏掉**
   - `img` 整個資料夾
   - `單檔版.html`（不上傳也行，那是給你寄 email 用的）
3. 下面 Commit message 隨便打（例如 `first`）→ 按 **Commit changes**

> **⚠ `.pages.yml` 上傳不了怎麼辦**
> Windows 檔案總管有時會擋住開頭是點的檔案。若拖不進去：
> 在 GitHub repo 頁面按 **Add file → Create new file**，
> 檔名打 `.pages.yml`，然後把我準備的那個檔用記事本打開、**全部複製貼上**，再 Commit。

## 步驟三：接上 Cloudflare Pages（拿到網址）

1. 到 **dash.cloudflare.com** → Sign up（同一個 email）
2. 左側選 **Workers & Pages** → **Create** → 切到 **Pages** 分頁 → **Connect to Git**
3. 授權 GitHub → 選你剛建的 `resume` repo
4. 建置設定**全部留空**：
   - Framework preset：`None`
   - Build command：**留空**
   - Build output directory：`/`
5. 按 **Save and Deploy**

約一分鐘後會給你網址，長這樣：
```
https://resume-xxxx.pages.dev
```
**這就是你可以拿去投履歷的連結。**

## 步驟四：開通後台

1. 到 **pagescms.org** → **Sign in with GitHub**
2. 授權它讀取你的 repo（可以只授權 `resume` 這一個）
3. 進去後選 `resume` → 左側會出現「**履歷內容**」
4. 點進去就能看到中文欄位：首屏、個人簡介、專業技能、工作經歷、作品、拍攝現場、聯絡

**改完按儲存 → Cloudflare 會自動重新部署 → 約 30–60 秒後線上網站就更新了。**

---

## 之後怎麼用

**改文字** → 後台對應欄位直接改
**換照片** → 欄位旁邊有上傳按鈕，選新圖就換掉
**新增一份工作經歷** → 「工作經歷」底下按 **＋ Add item**
**新增一張作品卡** → 「作品／頻道」底下按 **＋ Add item**
**刪除** → 每個項目右邊有刪除

---

## 兩個檔案的用途，別搞混

| 檔案 | 用途 |
|---|---|
| `index.html` ＋ `content.json` ＋ `img/` | **線上網站用。** 三個都要在 repo 裡，缺一個網站會壞 |
| `單檔版.html` | **寄 email／隨身碟用。** 照片嵌在裡面，丟到哪都能開，但不會跟著後台更新 |

> ⚠ **`index.html` 不能單獨雙擊開啟**——瀏覽器會禁止它讀取 `content.json`，畫面會顯示載入失敗。
> 要在本機看，就開 `單檔版.html`；要看最新版，就開你的線上網址。

---

## 可以之後再做的加分項

**接自己的網域**（例如 `tsaidinghsuan.com`）
Cloudflare Pages → 你的專案 → Custom domains → Set up a domain。網域一年約台幣 300–500，Cloudflare 自己也能買。
（Wix 之前提示 `tsai-ding-hsuan.com` 可用，你想要的話那個也行。）

**改主色**
後台「網站基本設定 → 主色」填色碼即可，例如 `#d4af37`（金）、`#ff6f4d`（現在的珊瑚橘）。

**把 `單檔版.html` 更新成最新內容**
後台改完之後，那個單檔版不會自動更新。需要的話跟我說，我重新產一份。

---

## 卡住的時候

哪一步出錯就截圖給我，或把錯誤訊息貼給我。最常見的兩個問題：

1. **網站是空白的／顯示「內容載入失敗」** → 通常是 `content.json` 沒上傳，或檔名打錯
2. **Pages CMS 看不到「履歷內容」** → 通常是 `.pages.yml` 沒上傳成功（見步驟二的提醒）
